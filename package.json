{
  "name": "@uploadcare/ai-image-editor-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI image generation and editing web component + React wrapper for Uploadcare",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace @uploadcare/ai-image-editor",
    "test": "npm run test --workspaces --if-present",
    "lint": "biome lint . && npm run lint:lit -w @uploadcare/ai-image-editor",
    "format": "biome format --write .",
    "prepare": "husky install",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "workspaces": [
    "packages/ai-image-editor",
    "packages/react-ai-image-editor"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.2.6",
    "@vitest/browser-playwright": "^4.1.10",
    "happy-dom": "^20.9.0",
    "husky": "^9.0.11",
    "lerna": "^9.0.3",
    "lint-staged": "^15.2.2",
    "typescript": "^5.6.0",
    "vite": "^6.2.3",
    "vite-plugin-dts": "^4.5.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "author": "Uploadcare",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/uploadcare/ai-image-editor.git"
  },
  "homepage": "https://github.com/uploadcare/ai-image-editor#readme",
  "bugs": {
    "url": "https://github.com/uploadcare/ai-image-editor/issues"
  }
}
